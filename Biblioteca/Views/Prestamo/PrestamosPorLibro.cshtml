@model IEnumerable<BiblioAwoo.Models.Prestamo>
<h2>Usuarios que tienen el libro: @ViewBag.Libro.Titulo</h2>

<table class="table">
    <thead>
    <tr>
        <th>Usuario</th>
        <th>Fecha préstamo</th>
        <th>Fecha devolución</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var p in Model)
    {
        <tr>
            <td>@p.Usuario.Nombre</td>
            <td>@p.FechaPrestamo.ToShortDateString()</td>
            <td>@p.FechaDevolucion.ToShortDateString()</td>
        </tr>
    }
    </tbody>
</table>

<a href="/Prestamo/Index" class="btn btn-secondary">Volver</a>